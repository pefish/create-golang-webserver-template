name: CI

on:
  push:
    branches:
      - 'test'

jobs:
  build:
    name: build
    runs-on: ubuntu-20.04

    steps:
      - name: executing remote ssh commands using ssh key
        uses: appleboy/ssh-action@v0.1.8
        with:
          host: ${{ secrets.HOST }}
          username: ubuntu
          key: ${{ secrets.KEY }}
          port: 22
          script: |
            cd {src}
            git checkout test
            ./script/ci-test.sh

